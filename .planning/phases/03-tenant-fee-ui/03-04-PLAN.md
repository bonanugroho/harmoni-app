---
phase: 03-tenant-fee-ui
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - apps/web/src/components/tenants/TenantCard.tsx
  - apps/web/src/components/tenants/TenantCard.test.tsx
  - apps/web/src/components/tenants/TenantForm.tsx
  - apps/web/src/components/tenants/TenantForm.test.tsx
  - apps/web/src/pages/TenantListPage.tsx
  - apps/web/src/pages/TenantListPage.test.tsx
  - apps/web/src/pages/TenantCreatePage.tsx
  - apps/web/src/pages/TenantCreatePage.test.tsx
  - apps/web/src/pages/TenantEditPage.tsx
  - apps/web/src/pages/TenantEditPage.test.tsx
autonomous: true
requirements:
  - TEN-01
  - FIN-01

must_haves:
  truths:
    - "TenantCard renders block, unit_number, occupancy badge, monthly_fee, fee summary, and is clickable"
    - "TenantForm renders fields: Block, Unit Number, Occupancy, Monthly Fee, dynamic Mandatory Fees section"
    - "TenantForm validates: block required, unit_number required, monthly_fee required/positive, >=1 mandatory fee"
    - "TenantForm adds fee rows via '+ Add Another Fee' button, removes rows via X button"
    - "TenantForm submits via useCreateTenant mutation and redirects to /tenants on success"
    - "TenantForm in edit mode (TenantEditPage) pre-populates fields and shows Delete Tenant button"
    - "TenantListPage fetches tenants via useTenants hook, shows loading skeleton, tenant cards, empty state, client-side search/filter"
    - "TenantListPage search filters by block and unit_number with 300ms debounce"
    - "TenantListPage responsive grid: 1 column mobile, 2 columns sm, 3 columns lg"
    - "TenantCreatePage renders TenantForm, cancel goes back to /tenants"
    - "TenantEditPage fetches tenant via useTenant, renders TenantForm in edit mode, delete shows ConfirmDialog"
    - "All pages and components have passing unit tests with mocked TQ hooks"
  artifacts:
    - path: "apps/web/src/components/tenants/TenantCard.tsx"
      provides: "Tenant summary card with occupancy badge, fee info, click handler"
      contains: "export default function TenantCard"
    - path: "apps/web/src/components/tenants/TenantForm.tsx"
      provides: "Create/edit tenant form with dynamic mandatory fee rows"
      contains: "export default function TenantForm"
    - path: "apps/web/src/pages/TenantListPage.tsx"
      provides: "Tenant list page with search/filter, responsive card grid"
      contains: "export default function TenantListPage"
    - path: "apps/web/src/pages/TenantCreatePage.tsx"
      provides: "Create tenant page wrapping TenantForm"
      contains: "export default function TenantCreatePage"
    - path: "apps/web/src/pages/TenantEditPage.tsx"
      provides: "Edit tenant page with pre-populated form and delete"
      contains: "export default function TenantEditPage"
  key_links:
    - from: "components/tenants/TenantCard.tsx"
      to: "pages/TenantDetailPage.tsx (Plan 05)"
      via: "/tenants/:id navigation on click"
    - from: "components/tenants/TenantForm.tsx"
      to: "components/ui/Input, Select, DatePicker"
      via: "import Input from '../ui/Input'"
    - from: "pages/TenantListPage.tsx"
      to: "hooks/useTenants.ts"
      via: "import { useTenants } from '../hooks/useTenants'"
---

<objective>
Build all tenant-related components and pages: TenantCard, TenantForm (with dynamic mandatory fees), TenantListPage, TenantCreatePage, TenantEditPage.

Purpose: Deliver the core tenant management UI — listing tenants with search/filter, creating tenants with mandatory fees inline, editing and deleting tenants. Consumes the hooks and services from earlier plans.

Output: 5 component/page files with 5 co-located test files.
</objective>

<execution_context>
@/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tenant-fee-ui/03-CONTEXT.md
@.planning/phases/03-tenant-fee-ui/03-RESEARCH.md
@.planning/phases/03-tenant-fee-ui/03-UI-SPEC.md
@.planning/phases/03-tenant-fee-ui/03-PATTERNS.md

<interfaces>
<!-- Contracts consumed by this plan -->

From hooks/useTenants.ts:
```typescript
export function useTenants(): { data?: Tenant[]; isLoading: boolean; isError: boolean; error: Error | null };
export function useTenant(id: string): { data?: Tenant; isLoading: boolean; isError: boolean; error: Error | null };
export function useCreateTenant(): { mutate: (data: CreateTenantRequest) => void; mutateAsync: (data: CreateTenantRequest) => Promise<Tenant>; isLoading: boolean; isError: boolean; error: Error | null };
export function useUpdateTenant(id: string): { mutate: (data: UpdateTenantRequest) => void; isLoading: boolean };
export function useDeleteTenant(): { mutate: (id: string) => void; isLoading: boolean };
```

From components/ui/:
```typescript
export default function Input(props: { label: string; error?: string } & InputHTMLAttributes);
export default function Select(props: { label: string; error?: string; options: Array<{value:string;label:string}>; placeholder?: string } & SelectHTMLAttributes);
export default function StatusBadge(props: { status: 'occupied' | 'vacant' | 'paid' | 'unpaid' });
export default function ConfirmDialog(props: { isOpen: boolean; title: string; message: string; confirmLabel?: string; cancelLabel?: string; onConfirm: () => void; onCancel: () => void; isLoading?: boolean });
export default function PageHeader(props: { title: string; action?: { label: string; onClick: () => void; icon?: ReactNode } });
export default function LoadingSkeleton(props: { variant: 'card' | 'list' | 'form'; count?: number });
export default function EmptyState(props: { heading: string; body: string; action?: { label: string; onClick: () => void } });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TenantCard component and TenantForm with tests</name>
  <files>
    apps/web/src/components/tenants/TenantCard.tsx
    apps/web/src/components/tenants/TenantCard.test.tsx
    apps/web/src/components/tenants/TenantForm.tsx
    apps/web/src/components/tenants/TenantForm.test.tsx
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-UI-SPEC.md (lines 239-284: tenant card content, layout; lines 286-320: tenant form layout; lines 137-199: copywriting contract for labels, validation messages; lines 389-397: form submission pattern)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/web/src/components/auth/LoginForm.tsx (existing form pattern — inline validation, loading/error state, prevent double-submit, disabled button)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-RESEARCH.md (lines 401-408: Pitfall 2 — stale query cache; lines 431-435: Pitfall 5 — formatIDR utility)
  </read_first>
  <action>
    Create a utility function at top of TenantCard.tsx (or a shared utility file if preferred for reuse):
    - `function formatIDR(amount: number): string` using `Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', minimumFractionDigits: 0 })` — returns "Rp50,000" format

    Create `TenantCard.tsx`:
    - Props: `tenant: Tenant`, `onClick?: () => void`
    - Renders `<div className="cursor-pointer rounded-lg border border-gray-200 bg-white p-4 shadow-sm transition-shadow hover:shadow-md active:scale-[0.98]" onClick={onClick} role="button" tabIndex={0} onKeyDown={(e) => e.key === 'Enter' && onClick?.()}>`
    - Top row: "Block {block} · Unit {unit_number}" (text-base font-semibold text-gray-900) + `<StatusBadge status={occupancy} />`
    - Middle: "{formatIDR(monthly_fee)} / month" (text-sm font-semibold text-gray-900 mt-2)
    - Bottom: fee summary text (text-sm text-gray-500 mt-1):
      - If fees exist: "N mandatory fees · M contributions"
      - If no fees: "No fees configured"
    - Card uses `min-h-[100px]` for consistent sizing

    Create `TenantForm.tsx`:
    - Props: `initialData?: Tenant` (for edit mode), `onSubmit: (data: CreateTenantRequest) => Promise<void>`, `isLoading?: boolean`
    - Internal state for form fields (useState):
      - `block`, `unit_number`, `occupancy` (default 'occupied'), `monthly_fee` (number)
      - `mandatoryFees`: Array<{ description: string; amount: number; effective_date: string }> (default: [{ description: '', amount: 0, effective_date: '' }])
      - `errors`: Record<string, string> (field validation errors)
      - `submitError`: string (server/API error)
    - If `initialData` provided, pre-populate all fields in a `useEffect` on mount
    - **Validation** (on submit):
      - `block` empty → "Block is required."
      - `unit_number` empty → "Unit number is required."
      - `monthly_fee` <= 0 → "Monthly fee must be a positive amount."
      - `mandatoryFees` length === 0 → "At least one mandatory fee is required."
      - Each fee: `description` empty → "Description is required."
      - Each fee: `amount` <= 0 → "Amount is required."
      - Each fee: `effective_date` empty → "Effective date is required."
      - Each fee: `amount > monthly_fee` → "Fee amount cannot exceed the tenant's monthly fee."
    - **UI Structure:**
      - Back link: "← Back to Tenants" (Link to /tenants, text-blue-600, text-sm)
      - Page heading: "Add New Tenant" or "Edit Unit {block}-{unit_number}" (text-2xl font-semibold text-gray-900 mt-4)
      - Form card: `<div className="mx-auto mt-6 max-w-lg space-y-6 rounded-lg border border-gray-200 bg-white p-6">`
      - Fields: `<Input name="block" label="Block" placeholder="A" value={block} onChange={...} error={errors.block} />` (same pattern for unit_number, monthly_fee)
      - Occupancy: `<Select name="occupancy" label="Occupancy Status" options={[{value:'occupied',label:'Occupied'},{value:'vacant',label:'Vacant'}]} value={occupancy} onChange={...} error={errors.occupancy} />`
      - Dynamic mandatory fees section:
        - Section heading: "Mandatory Fees" (text-sm font-semibold text-gray-700 mt-6 mb-4, with top border)
        - For each fee in `mandatoryFees`: container with fee fields (description input, amount input, effective_date input) + remove button (X icon from lucide-react, min-h-[44px] min-w-[44px], red-500 text, only shown if > 1 fee)
        - "+ Add Another Fee" button (text-blue-600 text-sm font-medium, mt-3, adds empty fee entry)
      - Error alert at top of form if `submitError` is set (`role="alert"`, red-50 bg, red-700 text)
      - Bottom buttons:
        - Cancel: Link to "/tenants", gray outline button, min-h-[44px]
        - Submit: blue-600 bg, white text, min-h-[44px], disabled when `isLoading`, shows spinner + "Saving..." when loading, label "Save Tenant" (create) or "Update Tenant" (edit)
    - On submit: call `onSubmit(data)`, which the parent page handles via the mutation hook

    Create `TenantCard.test.tsx`:
    - No mocks needed (pure display component)
    - Test 1: renders block, unit_number
    - Test 2: renders occupancy StatusBadge
    - Test 3: renders formatted monthly_fee (contains "Rp")
    - Test 4: renders fee summary text
    - Test 5: calls onClick when card is clicked
    - Test 6: calls onClick when Enter key is pressed
    - Render with MemoryRouter (since card may contain Link)

    Create `TenantForm.test.tsx`:
    - Mock `@tanstack/react-query` (useQuery, useMutation via `vi.mock`)
    - Mock `react-router-dom` (useNavigate via `vi.mock`)
    - Test 1: renders all form fields (block, unit_number, occupancy, monthly_fee)
    - Test 2: shows mandatory fee section with initial empty fee row
    - Test 3: adds fee row when "+ Add Another Fee" is clicked
    - Test 4: removes fee row when X button is clicked
    - Test 5: shows validation error for empty block on submit
    - Test 6: shows validation error for empty fee description on submit
    - Test 7: calls onSubmit with form data when validation passes
    - Test 8: shows submitError alert when provided
    - Test 9: pre-populates fields when initialData is provided (edit mode)
    - Render with MemoryRouter
  </action>
  <acceptance_criteria>
    - TenantCard renders block, unit_number, occupancy badge, monthly_fee (formatted as IDR), and fee summary
    - TenantCard is keyboard-accessible (Enter key triggers onClick)
    - TenantForm renders all input fields using shared Input/Select components
    - TenantForm dynamic fee section has add/remove functionality
    - TenantForm validates all required fields before submit
    - TenantForm validates fee amount <= monthly_fee cap
    - TenantForm shows spinner + "Saving..." during submit
    - `npx vitest run src/components/tenants/TenantCard.test.tsx` exits 0
    - `npx vitest run src/components/tenants/TenantForm.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/tenants/TenantCard.test.tsx src/components/tenants/TenantForm.test.tsx</automated>
  </verify>
  <done>TenantCard and TenantForm created with passing tests. Form has dynamic mandatory fee rows and validation.</done>
</task>

<task type="auto">
  <name>Task 2: Create TenantListPage with search/filter and responsive grid</name>
  <files>
    apps/web/src/pages/TenantListPage.tsx
    apps/web/src/pages/TenantListPage.test.tsx
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-UI-SPEC.md (lines 239-277: Tenant List page layout, card grid, search filter, empty state, loading state)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-RESEARCH.md (lines 638-691: test pattern with mocked useQuery)
  </read_first>
  <action>
    Create `TenantListPage.tsx`:
    - Imports: `useState`, `useMemo`, from react; `useNavigate` from react-router-dom; `Search` from lucide-react; `useTenants` from hooks; `TenantCard`, `PageHeader`, `LoadingSkeleton`, `EmptyState`, `Input` from components
    - **Data fetching:** `const { data: tenants, isLoading, isError, error } = useTenants()`
    - **Search state:** `searchQuery: string` state with `handleSearchChange` that updates state (debounce handled client-side via useEffect with 300ms setTimeout)
    - **Filtered tenants:** `useMemo(() => { if (!tenants) return []; if (!searchQuery) return tenants; const q = searchQuery.toLowerCase(); return tenants.filter(t => t.block.toLowerCase().includes(q) || t.unit_number.toLowerCase().includes(q)); }, [tenants, searchQuery])`
    - **Sorting:** sort by block then unit_number ascending (useMemo on filtered tenants)
    - **Layout:**
      - `<div className="space-y-6">` outer container
      - `<PageHeader title="Tenants" action={{ label: '+ Add Tenant', onClick: () => navigate('/tenants/new') }} />`
      - Search bar: `<Input name="search" label="" placeholder="Search by block or unit number..." value={searchQuery} onChange={...} className="max-w-md" />` with Search icon (lucide-react) positioned inside input area
      - **Loading state (isLoading):** `<LoadingSkeleton variant="card" count={3} />`
      - **Error state (isError):** `<div className="rounded-md bg-red-50 p-4 text-sm text-red-700">Failed to load tenants. Pull down to refresh or try again.</div>`
      - **Empty state (tenants length === 0 && !isLoading):** `<EmptyState heading="No Tenants Yet" body="Start by adding your first tenant to this RT." action={{ label: '+ Add Tenant', onClick: () => navigate('/tenants/new') }} />` — BUT if searchQuery is set and tenants exist but filtered list is empty, show "No tenants match your search" instead
      - **Normal state:** Grid of TenantCard components:
        - `<div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">`
        - Each card: `<TenantCard key={tenant.id} tenant={tenant} onClick={() => navigate(`/tenants/${tenant.id}`)} />`

    Create `TenantListPage.test.tsx`:
    - Mock `@tanstack/react-query` with `vi.mock` (same pattern as RESEARCH.md test example)
    - Mock `react-router-dom` (useNavigate) with `vi.mock`
    - Import `useQuery` from `@tanstack/react-query` after mock
    - Test 1: shows LoadingSkeleton when isLoading is true
    - Test 2: renders TenantCards when data loads
    - Test 3: shows EmptyState when data is empty array
    - Test 4: error state shows error message
    - Test 5: search filters by block name
    - Test 6: search filters by unit_number
    - Test 7: "+ Add Tenant" button navigates to /tenants/new
    - Each test sets `(useQuery as Mock).mockReturnValue(...)` with appropriate return values
    - `render` with MemoryRouter
  </action>
  <acceptance_criteria>
    - TenantListPage fetches data via `useTenants()` hook
    - Shows LoadingSkeleton during loading
    - Shows error message on error
    - Shows EmptyState when no tenants exist
    - Renders responsive card grid (1→2→3 columns)
    - Search filters client-side by block and unit_number
    - Search has 300ms debounce (useEffect with setTimeout)
    - Empty list with active search shows different message
    - Cards sort by block then unit_number ascending
    - `npx vitest run src/pages/TenantListPage.test.tsx` exits 0 (7 tests)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && npx vitest run src/pages/TenantListPage.test.tsx</automated>
  </verify>
  <done>TenantListPage created with responsive grid, search/filter, loading/empty/error states. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create TenantCreatePage and TenantEditPage with tests</name>
  <files>
    apps/web/src/pages/TenantCreatePage.tsx
    apps/web/src/pages/TenantCreatePage.test.tsx
    apps/web/src/pages/TenantEditPage.tsx
    apps/web/src/pages/TenantEditPage.test.tsx
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-UI-SPEC.md (lines 286-320: Tenant Create/Edit page layout with back link, form card, cancel/submit)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-UI-SPEC.md (lines 157-167: copywriting for edit page heading, delete confirmation)
  </read_first>
  <action>
    Create `TenantCreatePage.tsx`:
    - Imports: `useNavigate` from react-router-dom; `useCreateTenant` from hooks; `TenantForm` from components
    - Page wrapper: renders `<TenantForm onSubmit={handleSubmit} isLoading={isPending} />`
    - `handleSubmit`:
      - Calls `createTenantMutation.mutateAsync(data)`
      - On success: `navigate('/tenants')`
      - On error: re-throw so TenantForm shows server error
    - The mutation comes from `const mutation = useCreateTenant()`; `isPending` from `mutation.isPending` (TanStack Query v5 uses `isPending` not `isLoading` for mutations)

    Create `TenantCreatePage.test.tsx`:
    - Mock `@tanstack/react-query` (useMutation via `vi.mock`)
    - Mock `react-router-dom` (useNavigate)
    - Import `useMutation` from `@tanstack/react-query` after mock
    - Test 1: renders TenantForm inside a container
    - Test 2: submit calls mutation.mutateAsync with form data
    - Test 3: on success navigates to /tenants
    - `render` with MemoryRouter

    Create `TenantEditPage.tsx`:
    - Imports: `useParams`, `useNavigate` from react-router-dom; `useTenant`, `useUpdateTenant`, `useDeleteTenant` from hooks; `TenantForm`, `ConfirmDialog`, `LoadingSkeleton` from components
    - Gets `id` from `useParams<{ id: string }>()`
    - Fetches tenant: `const { data: tenant, isLoading, isError } = useTenant(id)`
    - Mutations: `const updateMutation = useUpdateTenant(id)` and `const deleteMutation = useDeleteTenant()`
    - State: `showDeleteDialog: boolean`
    - **Loading:** if `isLoading`, show `<LoadingSkeleton variant="form" />`
    - **Error/Not found:** if `isError` or no tenant, show error message + "Back to Tenants" link
    - **Normal:** renders:
      - `<TenantForm initialData={tenant} onSubmit={handleUpdate} isLoading={updateMutation.isPending} />`
      - Below form: danger zone — "Delete Tenant" button (red-600 outline, min-h-[44px], mt-8) that opens ConfirmDialog
      - `<ConfirmDialog isOpen={showDeleteDialog} title="Delete Unit {block}-{unit_number}?" message="This cannot be undone." onConfirm={handleDelete} onCancel={() => setShowDeleteDialog(false)} isLoading={deleteMutation.isPending} />`
    - `handleUpdate`: calls `updateMutation.mutateAsync(data)`, on success navigates to `/tenants`
    - `handleDelete`: calls `deleteMutation.mutateAsync(id)`, on success navigates to `/tenants`

    Create `TenantEditPage.test.tsx`:
    - Mock `@tanstack/react-query` (useQuery, useMutation)
    - Mock `react-router-dom` (useParams, useNavigate)
    - Test 1: shows LoadingSkeleton when tenant is loading
    - Test 2: renders TenantForm with initialData when tenant loads
    - Test 3: shows error when tenant fetch fails
    - Test 4: shows delete danger button
    - Test 5: opens ConfirmDialog when delete button clicked
    - Test 6: navigating on successful delete
    - Render with MemoryRouter
  </action>
  <acceptance_criteria>
    - TenantCreatePage renders TenantForm with no initialData (create mode)
    - TenantCreatePage calls useCreateTenant mutation on submit
    - TenantCreatePage navigates to /tenants on success
    - TenantEditPage fetches tenant by id via useTenant
    - TenantEditPage renders TenantForm with initialData (edit mode)
    - TenantEditPage shows delete button and ConfirmDialog for deletion
    - `npx vitest run src/pages/TenantCreatePage.test.tsx` exits 0 (3 tests)
    - `npx vitest run src/pages/TenantEditPage.test.tsx` exits 0 (6 tests)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && npx vitest run src/pages/TenantCreatePage.test.tsx src/pages/TenantEditPage.test.tsx</automated>
  </verify>
  <done>TenantCreatePage and TenantEditPage created with passing tests. Create/update/delete flows handled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TenantForm → mutation hook | Form data crosses from user input to TanStack Query mutation. Client-side validation runs before submission. |
| TenantListPage → useTenants hook | Query calls go through service layer to API. No direct fetch. |
| Delete flow | ConfirmDialog ensures user intent before delete mutation. Optimistic removal with error fallback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | TenantForm validation | mitigate | All fields validated client-side before submit: block/unit_number required, monthly_fee > 0, >=1 mandatory fee, fee amount <= monthly_fee. Server also validates (defense in depth). |
| T-03-12 | Elevation of Privilege | TenantEditPage delete | accept | Delete button visible to all roles with edit access. Backend Casbin enforcement prevents unauthorized deletes regardless of frontend UI state. |
| T-03-13 | Information Disclosure | TenantCard search | accept | Search filters client-side only. All tenants are already loaded and visible to authorized user. Backend enforces territory isolation. |
</threat_model>

<verification>
- `npx vitest run src/components/tenants/` — TenantCard and TenantForm tests pass
- `npx vitest run src/pages/TenantListPage.test.tsx` — list page tests pass
- `npx vitest run src/pages/TenantCreatePage.test.tsx` — create page tests pass
- `npx vitest run src/pages/TenantEditPage.test.tsx` — edit page tests pass
- All pages use TanStack Query hooks (no manual fetch)
- All forms use shared Input/Select/DatePicker from components/ui/ (D-14)
</verification>

<success_criteria>
1. TenantCard component renders tenant summary with occupancy badge and formatted fee
2. TenantForm with dynamic mandatory fee section and validation
3. TenantListPage with search/filter, responsive grid (1→2→3 columns), loading/empty/error states
4. TenantCreatePage creates tenant and redirects on success
5. TenantEditPage pre-populates form, deletes with confirmation
6. All 5 test files pass
</success_criteria>

<output>
Create `.planning/phases/03-tenant-fee-ui/03-04-SUMMARY.md` when done
</output>
