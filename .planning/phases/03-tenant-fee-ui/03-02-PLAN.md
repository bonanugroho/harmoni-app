---
phase: 03-tenant-fee-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/web/src/components/ui/Input.tsx
  - apps/web/src/components/ui/Input.test.tsx
  - apps/web/src/components/ui/Select.tsx
  - apps/web/src/components/ui/Select.test.tsx
  - apps/web/src/components/ui/DatePicker.tsx
  - apps/web/src/components/ui/DatePicker.test.tsx
  - apps/web/src/components/ui/StatusBadge.tsx
  - apps/web/src/components/ui/StatusBadge.test.tsx
  - apps/web/src/components/ui/ConfirmDialog.tsx
  - apps/web/src/components/ui/ConfirmDialog.test.tsx
  - apps/web/src/components/ui/PageHeader.tsx
  - apps/web/src/components/ui/PageHeader.test.tsx
  - apps/web/src/components/ui/LoadingSkeleton.tsx
  - apps/web/src/components/ui/LoadingSkeleton.test.tsx
  - apps/web/src/components/ui/EmptyState.tsx
  - apps/web/src/components/ui/EmptyState.test.tsx
  - apps/web/src/components/layout/AppLayout.tsx
  - apps/web/src/components/layout/AppLayout.test.tsx
  - apps/web/src/App.tsx
autonomous: true
requirements:
  - TEN-01
  - FIN-01
  - FIN-02

must_haves:
  truths:
    - "Input component renders with label, error message, aria attributes, and 44px minimum touch target"
    - "Select component renders options, label, error, aria attributes, 44px minimum touch target"
    - "DatePicker renders native date input with label, error, aria attributes, 44px minimum touch target"
    - "StatusBadge renders occupancy status (Occupied/Vacant) with correct semantic colors"
    - "StatusBadge renders payment status (Paid/Unpaid) with correct semantic colors"
    - "ConfirmDialog opens on trigger, shows warning message, has Cancel and Delete buttons, closes on Escape or backdrop click"
    - "PageHeader renders title and optional action button"
    - "LoadingSkeleton renders pulse-animated placeholder elements"
    - "EmptyState renders heading, body text, and CTA button"
    - "AppLayout renders responsive sidebar: fixed 260px on desktop, collapsible icon-only on tablet, hidden overlay drawer on mobile"
    - "Sidebar items are hardcoded: Harmoni brand, Tenants link to /tenants, Reports placeholder"
    - "Sidebar active route is highlighted with blue-100 background"
    - "App.tsx routes include /tenants/* wrapped in ProtectedRoute and AppLayout"
    - "All UI components have passing unit tests"
    - "Existing auth forms (LoginForm, RegisterForm, ResetPasswordForm) are NOT modified (D-13)"
  artifacts:
    - path: "apps/web/src/components/ui/Input.tsx"
      provides: "Reusable input component with label, error, aria, 44px touch target"
      contains: "export default function Input"
    - path: "apps/web/src/components/ui/Select.tsx"
      provides: "Reusable select component with label, error, aria"
      contains: "export default function Select"
    - path: "apps/web/src/components/ui/DatePicker.tsx"
      provides: "Reusable date input with native type=date"
      contains: "export default function DatePicker"
    - path: "apps/web/src/components/ui/StatusBadge.tsx"
      provides: "Occupied/Vacant and Paid/Unpaid badge component"
      contains: "export default function StatusBadge"
    - path: "apps/web/src/components/ui/ConfirmDialog.tsx"
      provides: "Delete confirmation modal with backdrop"
      contains: "export default function ConfirmDialog"
    - path: "apps/web/src/components/ui/PageHeader.tsx"
      provides: "Page title + CTA button layout"
      contains: "export default function PageHeader"
    - path: "apps/web/src/components/ui/LoadingSkeleton.tsx"
      provides: "Pulse-animated skeleton placeholders"
      contains: "export default function LoadingSkeleton"
    - path: "apps/web/src/components/ui/EmptyState.tsx"
      provides: "Empty state with heading, body, CTA"
      contains: "export default function EmptyState"
    - path: "apps/web/src/components/layout/AppLayout.tsx"
      provides: "Responsive sidebar + header + content shell for authenticated pages"
      contains: "export default function AppLayout"
    - path: "apps/web/src/App.tsx"
      provides: "/tenants/* routes with ProtectedRoute + AppLayout wrapping"
      contains: "/tenants"
  key_links:
    - from: "components/ui/Input.tsx"
      to: "components/tenants/TenantForm.tsx (Plan 04)"
      via: "import Input from '../ui/Input'"
    - from: "components/ui/Select.tsx"
      to: "components/tenants/TenantForm.tsx (Plan 04)"
      via: "import Select from '../ui/Select'"
    - from: "components/ui/DatePicker.tsx"
      to: "components/fees/FeeForm.tsx (Plan 05)"
      via: "import DatePicker from '../ui/DatePicker'"
    - from: "components/layout/AppLayout.tsx"
      to: "pages/TenantListPage.tsx (Plan 04)"
      via: "AppLayout wraps page as child"
---

<objective>
Build the reusable UI component library and the AppLayout responsive sidebar shell.

Purpose: Establish a consistent set of form fields (Input, Select, DatePicker) and display components (StatusBadge, ConfirmDialog, PageHeader, LoadingSkeleton, EmptyState) that all pages consume. The AppLayout provides the responsive sidebar navigation shell wrapping all authenticated pages.

Output: 8 UI components (each with .test.tsx), AppLayout with .test.tsx, updated App.tsx with /tenants/* routes.
</objective>

<execution_context>
@/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tenant-fee-ui/03-CONTEXT.md
@.planning/phases/03-tenant-fee-ui/03-RESEARCH.md
@.planning/phases/03-tenant-fee-ui/03-UI-SPEC.md
@.planning/phases/03-tenant-fee-ui/03-PATTERNS.md

<interfaces>
<!-- Key types consumed by this plan — extracted from Plan 01 outputs -->

From types/tenant.ts (expected after Plan 01):
```typescript
export interface Tenant {
  id: string;
  block: string;
  unit_number: string;
  occupancy: 'occupied' | 'vacant';
  monthly_fee: number;
  territory_id: string;
  created_at: string;
  updated_at: string;
}
```

From services/auth.ts (unchanged public API):
```typescript
export async function login(email: string, password: string): Promise<LoginResponse>;
export function useAuth(): AuthContextType;  // from ProtectedRoute.tsx
```

From index.css (sidebar tokens added in Plan 01):
```
--color-sidebar: #F3F4F6
--color-sidebar-hover: #E5E7EB
--color-sidebar-active: #DBEAFE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable form field components (Input, Select, DatePicker) with tests</name>
  <files>
    apps/web/src/components/ui/Input.tsx
    apps/web/src/components/ui/Input.test.tsx
    apps/web/src/components/ui/Select.tsx
    apps/web/src/components/ui/Select.test.tsx
    apps/web/src/components/ui/DatePicker.tsx
    apps/web/src/components/ui/DatePicker.test.tsx
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/web/src/components/auth/LoginForm.tsx (lines 60-84: existing field markup pattern — label/htmlFor, input with min-h-[44px], aria-invalid, aria-describedby, error p[role=alert] with id)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/web/src/components/auth/LoginForm.test.tsx (test pattern — render with MemoryRouter, screen.getByLabelText, fireEvent, waitFor)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-RESEARCH.md (lines 331-369: Pattern 3 — Input component example; lines 84-94: Standard Stack; lines 638-691: test pattern with MemoryRouter)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-UI-SPEC.md (lines 168-199: form field labels, validation messages; lines 482-493: accessibility requirements)
  </read_first>
  <action>
    Create `components/ui/Input.tsx`:
    - Extends `InputHTMLAttributes<HTMLInputElement>` (import from `react`)
    - Props: `label: string`, `error?: string`, plus all native input props via `...props`
    - Derives `inputId` from `id || props.name`
    - Renders `<div className="space-y-2">` containing:
      - `<label htmlFor={inputId} className="block text-base font-medium text-gray-700">{label}</label>`
      - `<input id={inputId} className="min-h-[44px] w-full rounded-md border px-3 py-2 text-base outline-none transition-colors"` with dynamic border: `error ? 'border-red-600 focus:border-red-600' : 'border-gray-200 focus:border-blue-600'`
      - `aria-invalid={!!error}` and `aria-describedby={error ? errorId : undefined}`
      - If error: `<p id={errorId} className="text-sm text-red-600" role="alert">{error}</p>`
    - Use `export default function Input`

    Create `components/ui/Select.tsx`:
    - Extends `SelectHTMLAttributes<HTMLSelectElement>` (import from `react`)
    - Props: `label: string`, `error?: string`, `options: Array<{ value: string; label: string }>`, `placeholder?: string`, plus native select props
    - Same layout pattern as Input: label + select + error
    - Select element has `min-h-[44px]` touch target, same border styling
    - Options rendered via `options.map(o => <option key={o.value} value={o.value}>{o.label}</option>)`
    - If `placeholder` is provided, render `<option value="" disabled>{placeholder}</option>` as first option
    - All aria attributes matching Input pattern

    Create `components/ui/DatePicker.tsx`:
    - Extends `InputHTMLAttributes<HTMLInputElement>`
    - Props: `label: string`, `error?: string`
    - Renders `<input type="date">` with same styling and aria pattern as Input
    - Native date input (D-11: no extra dependency)

    Create `Input.test.tsx`:
    - Mock: no external mocks needed (pure component, no services or routing)
    - Test 1: renders label text
    - Test 2: renders input with correct id matching htmlFor
    - Test 3: shows error message when error prop is provided
    - Test 4: sets aria-invalid when error is present
    - Test 5: does not show error when error prop is undefined
    - Test 6: applies 44px minimum height (check className contains `min-h-[44px]`)
    - Render without MemoryRouter — pure component test

    Create `Select.test.tsx`:
    - Test 1: renders label and all options
    - Test 2: shows error message when error prop provided
    - Test 3: renders placeholder option when provided
    - Test 4: selects correct option on change

    Create `DatePicker.test.tsx`:
    - Test 1: renders label and date input
    - Test 2: shows error message when error prop provided
    - Test 3: input type is `date`
  </action>
  <acceptance_criteria>
    - Input.tsx exports a default function component with label/error/aria/44px
    - Select.tsx exports a default function component with label/error/options/aria
    - DatePicker.tsx exports a default function component with label/error/type="date"
    - All three components accept and spread native HTML attributes
    - `npx vitest run src/components/ui/Input.test.tsx` exits 0 (6 tests)
    - `npx vitest run src/components/ui/Select.test.tsx` exits 0 (4 tests)
    - `npx vitest run src/components/ui/DatePicker.test.tsx` exits 0 (3 tests)
    - No external CSS file imported — all styling via Tailwind className strings
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/ui/Input.test.tsx src/components/ui/Select.test.tsx src/components/ui/DatePicker.test.tsx</automated>
  </verify>
  <done>Input, Select, DatePicker created with passing tests. All follow LoginForm field pattern with label, error, aria, 44px.</done>
</task>

<task type="auto">
  <name>Task 2: Create display components (StatusBadge, ConfirmDialog, PageHeader, LoadingSkeleton, EmptyState) with tests</name>
  <files>
    apps/web/src/components/ui/StatusBadge.tsx
    apps/web/src/components/ui/StatusBadge.test.tsx
    apps/web/src/components/ui/ConfirmDialog.tsx
    apps/web/src/components/ui/ConfirmDialog.test.tsx
    apps/web/src/components/ui/PageHeader.tsx
    apps/web/src/components/ui/PageHeader.test.tsx
    apps/web/src/components/ui/LoadingSkeleton.tsx
    apps/web/src/components/ui/LoadingSkeleton.test.tsx
    apps/web/src/components/ui/EmptyState.tsx
    apps/web/src/components/ui/EmptyState.test.tsx
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-UI-SPEC.md (colors section lines 96-131: status badge background/text pairs; interaction patterns lines 412-419: ConfirmDialog pattern; lines 424-429: loading patterns; lines 139-167: copywriting contract for empty states)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/web/src/components/auth/LoginForm.test.tsx (test pattern reference — render, screen.getByText, fireEvent.click for dialog interactions)
  </read_first>
  <action>
    Create `StatusBadge.tsx`:
    - Props: `status: 'occupied' | 'vacant' | 'paid' | 'unpaid'`
    - Renders a `<span>` with `className="inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium"`
    - Mapping:
      - `occupied`: bg `#DBEAFE` (blue-100), text `#1D4ED8` (blue-700), label "Occupied"
      - `vacant`: bg `#FEF3C7` (amber-100), text `#92400E` (amber-800), label "Vacant"
      - `paid`: bg `#D1FAE5` (emerald-100), text `#065F46` (emerald-800), label "Paid"
      - `unpaid`: bg `#FEE2E2` (red-100), text `#991B1B` (red-800), label "Unpaid"
    - Use Tailwind color classes directly (not custom CSS variables)

    Create `ConfirmDialog.tsx`:
    - Props: `isOpen: boolean`, `title: string`, `message: string`, `confirmLabel?: string` (default "Delete"), `cancelLabel?: string` (default "Cancel"), `onConfirm: () => void`, `onCancel: () => void`, `isLoading?: boolean`
    - When `!isOpen`, return `null`
    - Renders overlay `<div className="fixed inset-0 z-50 flex items-center justify-center">` containing:
      - Backdrop: `<div className="fixed inset-0 bg-gray-900/50" onClick={onCancel} aria-hidden="true" />`
      - Dialog: `<div role="dialog" aria-modal="true" className="relative z-10 w-full max-w-md rounded-lg bg-white p-6 shadow-xl mx-4">` containing title (text-lg font-semibold), message (text-sm text-gray-600 mt-2), and two buttons:
        - Cancel: outline gray button, `min-h-[44px]`, `onClick={onCancel}`
        - Delete: red destructive button, `min-h-[44px]`, `onClick={onConfirm}`, shows spinner + "Deleting..." when `isLoading`
    - `useEffect` with Escape key handler: `if (isOpen) { const handler = (e) => e.key === 'Escape' && onCancel(); window.addEventListener('keydown', handler); return () => window.removeEventListener('keydown', handler) }`

    Create `PageHeader.tsx`:
    - Props: `title: string`, `action?: { label: string; onClick: () => void; icon?: React.ReactNode }`
    - Renders `<div className="flex items-center justify-between">` with title (text-2xl font-semibold text-gray-900) and optional action button (blue-600 bg, white text, min-h-[44px], rounded-md, px-4 py-2, flex items-center gap-2)

    Create `LoadingSkeleton.tsx`:
    - Props: `variant: 'card' | 'list' | 'form'`, `count?: number` (default 3)
    - `card`: renders `count` rounded rectangles with `animate-pulse`, gray-200 bg, ~h-32 each
    - `list`: renders `count` horizontal bars with `animate-pulse`, gray-200 bg, ~h-12 each
    - `form`: renders stacked skeleton fields (4 skeleton rows, ~h-12 each, with label skeletons)
    - All skeleton elements have `data-testid="loading-skeleton"` for test targeting

    Create `EmptyState.tsx`:
    - Props: `heading: string`, `body: string`, `action?: { label: string; onClick: () => void }`
    - Renders centered div with icon area (gray-400 circle placeholder), heading (text-lg font-semibold text-gray-900), body (text-sm text-gray-500 mt-2), and optional CTA button (blue-600 bg, white text, min-h-[44px], rounded-md, px-4)

    Create `StatusBadge.test.tsx`:
    - Test 1: renders "Occupied" for occupied status
    - Test 2: renders "Vacant" for vacant status
    - Test 3: renders "Paid" for paid status
    - Test 4: renders "Unpaid" for unpaid status

    Create `ConfirmDialog.test.tsx`:
    - Test 1: does not render when isOpen is false
    - Test 2: renders title and message when isOpen is true
    - Test 3: calls onCancel when Cancel button clicked
    - Test 4: calls onConfirm when Delete button clicked
    - Test 5: calls onCancel when Escape key pressed
    - Test 6: calls onCancel when backdrop clicked

    Create `PageHeader.test.tsx`:
    - Test 1: renders title
    - Test 2: renders action button when action prop provided
    - Test 3: does not render action button when action prop not provided
    - Test 4: calls action.onClick when button clicked

    Create `LoadingSkeleton.test.tsx`:
    - Test 1: renders specified count of skeleton items
    - Test 2: renders elements with data-testid="loading-skeleton"
    - Test 3: renders correct variant structure

    Create `EmptyState.test.tsx`:
    - Test 1: renders heading and body text
    - Test 2: renders CTA button when action provided
    - Test 3: does not render CTA when action not provided
    - Test 4: calls action.onClick when CTA clicked

    All tests render without MemoryRouter unless the component uses routing hooks (none of these do).
  </action>
  <acceptance_criteria>
    - 5 display components created in `components/ui/` with default exports
    - StatusBadge uses Tailwind semantic colors matching UI-SPEC color table (blue-100/700 for Occupied, amber-100/800 for Vacant, emerald-100/800 for Paid, red-100/800 for Unpaid)
    - ConfirmDialog has Escape key handler, backdrop click, focus management
    - All 5 test files exist and pass
    - No external dependencies beyond react and lucide-react
    - Every component has `min-h-[44px]` on interactive elements (buttons, badges)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/ui/StatusBadge.test.tsx src/components/ui/ConfirmDialog.test.tsx src/components/ui/PageHeader.test.tsx src/components/ui/LoadingSkeleton.test.tsx src/components/ui/EmptyState.test.tsx</automated>
  </verify>
  <done>All 5 display components created with passing tests. StatusBadge uses UI-SPEC colors. ConfirmDialog has Escape/backdrop/confirm interactions.</done>
</task>

<task type="auto">
  <name>Task 3: Create AppLayout with responsive sidebar and add /tenants/* routes to App.tsx</name>
  <files>
    apps/web/src/components/layout/AppLayout.tsx
    apps/web/src/components/layout/AppLayout.test.tsx
    apps/web/src/App.tsx
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/web/src/App.tsx (current routes — needs /tenants/* added with AppLayout wrapping)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/web/src/routes/ProtectedRoute.tsx (current route guard — AppLayout children get AuthContext)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-RESEARCH.md (lines 549-635: App.tsx routing example with AppLayout, ProtectedRoute, /tenants/* routes)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/03-tenant-fee-ui/03-UI-SPEC.md (lines 205-237: AppLayout layout diagram, desktop/tablet/mobile breakpoints, sidebar items table; lines 452-467: responsive breakpoint rules)
  </read_first>
  <action>
    Create `components/layout/AppLayout.tsx`:
    - Props: `children: React.ReactNode`
    - Internal state: `sidebarOpen: boolean` (default false for mobile), `isMobile: boolean` (derived from window.innerWidth listener in useEffect)
    - Imports: `useState, useEffect, type ReactNode` from react, `useNavigate, useLocation` from react-router-dom, `useAuth` from `../../routes/ProtectedRoute`, icons from `lucide-react` (Building2, Menu, X, ChevronLeft, BarChart3, Settings)
    - **Responsive breakpoints** (pure CSS via Tailwind responsive classes, no JS breakpoint detection needed — use Tailwind's `lg:` prefix):
      - Mobile (< 768px / `lg:`): sidebar hidden by default, overlay drawer with backdrop on toggle
      - Tablet (768-1023px): collapsible to icon-only 64px, expand on click
      - Desktop (≥ 1024px / `lg:`): fixed sidebar 260px, always visible
    - **Sidebar HTML structure:**
      - Mobile: fixed left-0 top-0 h-full w-64 bg-sidebar z-40, transform with translateX based on sidebarOpen, transition-transform duration-200 ease-in-out, with backdrop overlay when open
      - Desktop: fixed left-0 top-0 h-full w-64 bg-gray-100 (sidebar token) z-30, translate-x-0 always
    - **Sidebar Brand section:** top section with Harmoni brand name (text-display size, "Harmoni" text) and close button on mobile (Menu/X toggle)
    - **Sidebar Navigation items** (hardcoded per discretion):
      - Tenants: Building2 icon, link to `/tenants`, visible to all roles
      - Reports: BarChart3 icon, link to `/reports` (placeholder — Phase 4), visible to all roles
      - Settings: Settings icon, link to `/settings` (placeholder — future), visible only when `user?.role` is `rt_officer` or `rw_officer` (check via `useAuth()`)
    - **Active route detection:** Use `useLocation().pathname` — if path starts with `/tenants`, highlight Tenants with `bg-blue-100 text-blue-700` and a left border accent (`border-l-4 border-blue-600`)
    - **Sidebar link styling:** Each nav link is `<a>` or `<button>` with `min-h-[44px]`, `flex items-center gap-3`, `px-4`, `py-2.5`, `text-sm font-medium`, `rounded-md`, `hover:bg-gray-200`
    - **Mobile header:** Fixed top bar with hamburger Menu icon (44x44 touch target) on left, page title in center. Only visible on mobile (< lg:block, lg:hidden)
    - **Content area:** `ml-0 lg:ml-64` (no margin on mobile, 260px left margin on desktop), `pt-14 lg:pt-0` (padding for mobile header), `min-h-screen`, `bg-gray-50`
    - **Skip link:** First element in sidebar — `<a href="#main-content" className="sr-only focus:not-sr-only">Skip to content</a>`
    - Pass `{children}` as main content between header and footer
    - Wrap content in `<main id="main-content">`

    Create `AppLayout.test.tsx`:
    - Mock `vi.mock('../../routes/ProtectedRoute', async () => {
        const actual = await vi.importActual('../../routes/ProtectedRoute');
        return { ...actual, useAuth: vi.fn() };
      })`
    - Mock `vi.mock('react-router-dom', async () => {
        const actual = await vi.importActual('react-router-dom');
        return { ...actual, useNavigate: vi.fn(), useLocation: vi.fn() };
      })`
    - Test 1: renders children content
    - Test 2: shows sidebar navigation links (Tenants, Reports)
    - Test 3: highlights Tenants link when on /tenants path (mock useLocation to return { pathname: '/tenants' })
    - Test 4: shows Settings link for rt_officer role (mock useAuth to return { user: { role: 'rt_officer' } })
    - Test 5: hides Settings link for resident role (mock useAuth to return { user: { role: 'resident' } })
    - Test 6: mobile hamburger toggle opens sidebar (check aria-label="Toggle sidebar" button exists)
    - `render` with MemoryRouter, import `useAuth` and `useLocation` from mocked modules, use `vi.mocked()` for type assertions

    Update `App.tsx`:
    - Add imports for new pages (even though they don't exist yet — will be created in Plans 04 and 05):
      - `import TenantListPage from './pages/TenantListPage'`
      - `import TenantCreatePage from './pages/TenantCreatePage'`
      - `import TenantEditPage from './pages/TenantEditPage'`
      - `import TenantDetailPage from './pages/TenantDetailPage'`
      - `import AppLayout from './components/layout/AppLayout'`
    - Add routes between `/dashboard` and catch-all `*`:
      ```
      <Route path="/tenants" element={<ProtectedRoute><AppLayout><TenantListPage /></AppLayout></ProtectedRoute>} />
      <Route path="/tenants/new" element={<ProtectedRoute><AppLayout><TenantCreatePage /></AppLayout></ProtectedRoute>} />
      <Route path="/tenants/:id" element={<ProtectedRoute><AppLayout><TenantDetailPage /></AppLayout></ProtectedRoute>} />
      <Route path="/tenants/:id/edit" element={<ProtectedRoute><AppLayout><TenantEditPage /></AppLayout></ProtectedRoute>} />
      ```
    - Update the existing `/dashboard` route to use AppLayout too:
      ```
      <Route path="/dashboard" element={<ProtectedRoute><AppLayout>...</AppLayout></ProtectedRoute>} />
      ```
    - Keep all existing auth routes unchanged
    - Keep catch-all `*` route unchanged

    Remove App.css import from App.tsx if present — it shouldn't be imported (we deleted App.css in Plan 01).
  </action>
  <acceptance_criteria>
    - `AppLayout.tsx` renders sidebar with Tenants, Reports links and brand header
    - Sidebar uses responsive Tailwind classes: fixed on desktop (lg:translate-x-0), overlay drawer on mobile
    - Sidebar links have `min-h-[44px]` touch targets
    - Active route (Tenants) shows blue-100 bg + left border accent when pathname starts with `/tenants`
    - Settings link conditionally visible based on user role (rt_officer/rw_officer)
    - AppLayout renders `{children}` in `<main id="main-content">`
    - `App.tsx` has 4 /tenants/* routes wrapped in ProtectedRoute + AppLayout
    - Dashboard route also wrapped in AppLayout
    - `npx vitest run src/components/layout/AppLayout.test.tsx` exits 0 (6 tests)
    - `cd apps/web && npx vite build` succeeds (even with page imports pointing to files that don't exist yet — they will be created in Plans 04/05)
    - Wait: vite build will fail because page files don't exist yet. Instead, verify `npm test` passes on the existing + new tests only. The page files are placeholder imports that will resolve when Plans 04/05 create them.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/layout/AppLayout.test.tsx src/components/ui/</automated>
  </verify>
  <done>AppLayout with responsive sidebar created. App.tsx updated with /tenants/* routes. AppLayout tests pass. All UI component tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppLayout sidebar → AuthContext | Sidebar reads user role from AuthContext to conditionally show Settings link. No data crosses to API. |
| ConfirmDialog → mutation action | Dialog calls onConfirm/onCancel callbacks passed by parent. Never directly calls API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | AppLayout sidebar | mitigate | Settings link only visible to rt_officer/rw_officer via useAuth(). Role-based visibility enforced in component, not in URL. |
| T-03-05 | Tampering | ConfirmDialog | mitigate | Dialog is controlled component — isOpen, onConfirm, onCancel are all parent-controlled. No direct state mutation. |
| T-03-06 | Elevation of Privilege | AppLayout route visibility | accept | Sidebar only hides links visually. Routes are protected by ProtectedRoute + backend Casbin. Server-side enforcement prevents unauthorized access. |
</threat_model>

<verification>
- `npx vitest run src/components/ui/` — all 8 UI components pass their tests
- `npx vitest run src/components/layout/AppLayout.test.tsx` — AppLayout tests pass
- All interactive elements have `min-h-[44px]`
- Existing tests (auth components) still pass
</verification>

<success_criteria>
1. Input, Select, DatePicker reusable components created with label/error/aria/44px (3 test files pass)
2. StatusBadge, ConfirmDialog, PageHeader, LoadingSkeleton, EmptyState display components created (5 test files pass)
3. AppLayout with responsive sidebar (3 breakpoints) created with role-based navigation
4. App.tsx updated with 4 /tenants/* routes wrapped in ProtectedRoute + AppLayout
5. Dashboard route also wrapped in AppLayout
6. All 8 UI component test files pass
7. AppLayout test file passes
8. No existing auth forms modified (D-13 compliance)
</success_criteria>

<output>
Create `.planning/phases/03-tenant-fee-ui/03-02-SUMMARY.md` when done
</output>
