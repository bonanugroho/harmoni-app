---
phase: 02-tenant-fee-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/go.mod
  - apps/api/go.sum
  - apps/api/internal/domain/entity/tenant.go
  - apps/api/migrations/006_create_tenants_table.up.sql
  - apps/api/migrations/006_create_tenants_table.down.sql
  - apps/api/migrations/007_create_mandatory_fees_table.up.sql
  - apps/api/migrations/007_create_mandatory_fees_table.down.sql
  - apps/api/migrations/008_create_voluntary_fees_table.up.sql
  - apps/api/migrations/008_create_voluntary_fees_table.down.sql
  - apps/api/migrations/009_create_user_tenants_table.up.sql
  - apps/api/migrations/009_create_user_tenants_table.down.sql
autonomous: true
requirements:
  - TEN-01
  - FIN-01
  - FIN-02

must_haves:
  truths:
    - "Tenant entity exists with block, unit_number, occupancy, monthly_fee, territory_id fields"
    - "MandatoryFee entity exists linked to tenant with amount, description, effective_date, paid_at"
    - "VoluntaryFee entity exists linked to tenant with amount, description, effective_date, paid_at"
    - "tenants table exists with UNIQUE(block, unit_number, territory_id) constraint"
    - "mandatory_fees table exists with FK to tenants"
    - "voluntary_fees table exists with FK to tenants"
    - "user_tenants junction table exists linking users to tenants"
    - "shopspring/decimal package is available in go.mod"
  artifacts:
    - path: "apps/api/internal/domain/entity/tenant.go"
      provides: "Tenant, MandatoryFee, VoluntaryFee struct definitions with JSON tags and Sanitize methods"
      contains: "type Tenant struct"
    - path: "apps/api/migrations/006_create_tenants_table.up.sql"
      provides: "tenants table DDL with uuidv7 PK, territory FK, UNIQUE constraint"
      contains: "CREATE TABLE tenants"
    - path: "apps/api/migrations/007_create_mandatory_fees_table.up.sql"
      provides: "mandatory_fees table DDL with FK to tenants"
      contains: "CREATE TABLE mandatory_fees"
    - path: "apps/api/migrations/008_create_voluntary_fees_table.up.sql"
      provides: "voluntary_fees table DDL with FK to tenants"
      contains: "CREATE TABLE voluntary_fees"
    - path: "apps/api/migrations/009_create_user_tenants_table.up.sql"
      provides: "user_tenants junction table DDL"
      contains: "CREATE TABLE user_tenants"
  key_links:
    - from: "internal/domain/entity/tenant.go"
      to: "internal/infrastructure/repository/tenant_repository.go"
      via: "import and type alias"
    - from: "migrations/006_create_tenants_table.up.sql"
      to: "migrations/006_create_tenants_table.down.sql"
      via: "DROP TABLE IF EXISTS tenants CASCADE"
---

<objective>
Create database foundations for Phase 2: entity type definitions (Tenant, MandatoryFee, VoluntaryFee), SQL migrations for the four new tables (tenants, mandatory_fees, voluntary_fees, user_tenants), and install the shopspring/decimal dependency.

Purpose: Establish the data model and persistence schema that all subsequent backend layers (repositories, services, handlers) depend on.
Output: Go entity structs + 8 migration SQL files (4 up, 4 down) + updated go.mod/go.sum.
</objective>

<execution_context>
@/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tenant-fee-management/01-CONTEXT.md
@.planning/phases/02-tenant-fee-management/01-RESEARCH.md
@.planning/phases/02-tenant-fee-management/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shopspring/decimal and create Tenant/MandatoryFee/VoluntaryFee entity types</name>
  <files>
    apps/api/go.mod
    apps/api/go.sum
    apps/api/internal/domain/entity/tenant.go
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/go.mod (existing module definition, current dependencies)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/internal/domain/entity/user.go (existing entity pattern — Sanitize, JSON tags, package entity, struct with time.Time fields)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/02-tenant-fee-management/02-PATTERNS.md (lines 38-81: Tenant, MandatoryFee, VoluntaryFee struct pattern)
  </read_first>
  <action>
    Run `cd apps/api && go get github.com/shopspring/decimal` to install the decimal package. Then create `internal/domain/entity/tenant.go` in package `entity` containing three structs:

    1. `Tenant` with fields: `ID string` (json:"id"), `Block string` (json:"block"), `UnitNumber string` (json:"unit_number"), `Occupancy string` (json:"occupancy"), `MonthlyFee decimal.Decimal` (json:"monthly_fee"), `TerritoryID string` (json:"territory_id"), `CreatedAt time.Time` (json:"created_at"), `UpdatedAt time.Time` (json:"updated_at"). Include a `Sanitize() *Tenant` method that returns a copy with all fields (no sensitive fields to strip, unlike User).

    2. `MandatoryFee` with fields: `ID string` (json:"id"), `TenantID string` (json:"tenant_id"), `Amount decimal.Decimal` (json:"amount"), `Description string` (json:"description"), `EffectiveDate time.Time` (json:"effective_date"), `PaidAt *time.Time` (json:"paid_at"`), `CreatedAt time.Time` (json:"created_at"), `UpdatedAt time.Time` (json:"updated_at"). Include a `Sanitize() *MandatoryFee` method.

    3. `VoluntaryFee` with fields: `ID string` (json:"id"), `TenantID string` (json:"tenant_id"), `Amount decimal.Decimal` (json:"amount"), `Description string` (json:"description"), `EffectiveDate time.Time` (json:"effective_date"), `PaidAt *time.Time` (json:"paid_at"), `CreatedAt time.Time` (json:"created_at"), `UpdatedAt time.Time` (json:"updated_at"). Include a `Sanitize() *VoluntaryFee` method.

    Import paths: `"harmoni-api/internal/domain/entity"`, `"github.com/shopspring/decimal"`, `"time"`. Use `decimal.Decimal` (not float64) for all monetary fields per RESEARCH.md — financial precision requirement.

    Follow the exact User entity pattern from `user.go`: package declaration, imports, struct with json tags, Sanitize() method returning a new struct with all public fields copied.
  </action>
  <acceptance_criteria>
    - `cd apps/api && go build ./...` compiles without errors
    - `go.mod` contains `github.com/shopspring/decimal` entry
    - `tenant.go` exports three types: `Tenant`, `MandatoryFee`, `VoluntaryFee`
    - All monetary fields use `decimal.Decimal` not `float64`
    - Each struct has a `Sanitize()` method returning a non-nil pointer
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && go build ./... && go vet ./internal/domain/entity/...</automated>
  </verify>
  <done>Tenant, MandatoryFee, VoluntaryFee entities compile and pass vet. shopspring/decimal in go.mod.</done>
</task>

<task type="auto">
  <name>Task 2: Create SQL migration files for tenants, mandatory_fees, voluntary_fees, and user_tenants tables</name>
  <files>
    apps/api/migrations/006_create_tenants_table.up.sql
    apps/api/migrations/006_create_tenants_table.down.sql
    apps/api/migrations/007_create_mandatory_fees_table.up.sql
    apps/api/migrations/007_create_mandatory_fees_table.down.sql
    apps/api/migrations/008_create_voluntary_fees_table.up.sql
    apps/api/migrations/008_create_voluntary_fees_table.down.sql
    apps/api/migrations/009_create_user_tenants_table.up.sql
    apps/api/migrations/009_create_user_tenants_table.down.sql
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/migrations/002_create_users_table.up.sql (existing DDL pattern — uuidv7 PK, TIMESTAMP defaults, REFERENCES syntax)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/migrations/002_create_users_table.down.sql (DROP TABLE IF EXISTS ... CASCADE)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/02-tenant-fee-management/02-PATTERNS.md (lines 653-694: migration naming convention, DDL patterns, field types)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/02-tenant-fee-management/01-CONTEXT.md (lines 19-22: fee table schema decisions; lines 33-37: validation constraints)
  </read_first>
  <action>
    Create 8 migration files (4 up, 4 down) following the exact naming convention from Phase 1 (`NNN_description.up.sql` / `NNN_description.down.sql`):

    Migration 006 — `tenants` table:
    - up: `CREATE TABLE IF NOT EXISTS tenants (id UUID PRIMARY KEY DEFAULT uuidv7(), block VARCHAR(10) NOT NULL, unit_number VARCHAR(10) NOT NULL, occupancy VARCHAR(20) NOT NULL CHECK (occupancy IN ('occupied', 'vacant')), monthly_fee NUMERIC(12,2) NOT NULL, territory_id VARCHAR(50) NOT NULL REFERENCES territories(id), created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), UNIQUE(block, unit_number, territory_id))`. Add index: `CREATE INDEX idx_tenants_territory ON tenants(territory_id)`.
    - down: `DROP TABLE IF EXISTS tenants CASCADE`

    Migration 007 — `mandatory_fees` table:
    - up: `CREATE TABLE IF NOT EXISTS mandatory_fees (id UUID PRIMARY KEY DEFAULT uuidv7(), tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE, amount NUMERIC(12,2) NOT NULL CHECK (amount >= 0), description TEXT NOT NULL, effective_date DATE NOT NULL, paid_at TIMESTAMPTZ, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`. Add index: `CREATE INDEX idx_mandatory_fees_tenant ON mandatory_fees(tenant_id)`.
    - down: `DROP TABLE IF EXISTS mandatory_fees CASCADE`

    Migration 008 — `voluntary_fees` table:
    - up: Same schema as mandatory_fees but table name is `voluntary_fees`. Amount is `NUMERIC(12,2) NOT NULL CHECK (amount >= 0)`. Tenant FK with CASCADE delete.
    - down: `DROP TABLE IF EXISTS voluntary_fees CASCADE`

    Migration 009 — `user_tenants` junction table:
    - up: `CREATE TABLE IF NOT EXISTS user_tenants (user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE, tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), PRIMARY KEY (user_id, tenant_id))`. Add indexes: `CREATE INDEX idx_user_tenants_user ON user_tenants(user_id)` and `CREATE INDEX idx_user_tenants_tenant ON user_tenants(tenant_id)`.
    - down: `DROP TABLE IF EXISTS user_tenants CASCADE`
  </action>
  <acceptance_criteria>
    - 8 migration files exist in `apps/api/migrations/` following the `NNN_description.{up,down}.sql` pattern
    - Each up file creates its table with `IF NOT EXISTS` guard
    - Each down file uses `DROP TABLE IF EXISTS ... CASCADE`
    - `tenants` table has UNIQUE constraint on (block, unit_number, territory_id)
    - `mandatory_fees` and `voluntary_fees` have FK to `tenants` with `ON DELETE CASCADE`
    - `mandatory_fees` and `voluntary_fees` have `CHECK (amount >= 0)`
    - `user_tenants` has composite PRIMARY KEY (user_id, tenant_id)
  </acceptance_criteria>
  <verify>
    <automated>ls apps/api/migrations/006_* apps/api/migrations/007_* apps/api/migrations/008_* apps/api/migrations/009_* 2>/dev/null | wc -l</automated>
  </verify>
  <done>All 8 migration files exist with correct DDL. Entity types compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | SQL queries cross from application to PostgreSQL. Parameterized queries via pgx prevent SQL injection. |
| Service → Repository | Business logic calls data access layer. Territory filtering must be enforced here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | tenants table | mitigate | Migration 006 duplicates territory_id in tenants table for repo-level filtering. `WHERE territory_id = $1` enforced in repository layer. |
| T-02-03 | Tampering | migrations SQL | mitigate | All migration SQL uses parameterized schema creation only. No user-supplied data in migrations. pgx parameterized queries at runtime. |
| T-02-SC | Tampering | shopspring/decimal install | mitigate | Package verified: 7.4k GH stars, 10 yrs maintenance, Go module checksum via go.sum. slopcheck not applicable (Go ecosystem uses go mod verify). |
</threat_model>

<verification>
- `cd apps/api && go build ./...` completes without errors
- `go vet ./internal/domain/entity/...` passes
- `grep -c "shopspring/decimal" go.mod` returns 1
- `go mod verify` reports "all modules verified"
- All 8 migration files are syntactically valid SQL
</verification>

<success_criteria>
1. Entity types (Tenant, MandatoryFee, VoluntaryFee) defined with decimal.Decimal for monetary fields
2. Four up-migrations and four down-migrations created with correct schemas
3. shopspring/decimal dependency installed and verified
4. `go build` passes with no errors
5. No files outside the specified set were modified
</success_criteria>

<output>
Create `.planning/phases/02-tenant-fee-management/02-01-SUMMARY.md` when done
</output>
