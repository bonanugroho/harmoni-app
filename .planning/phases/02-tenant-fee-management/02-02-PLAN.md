---
phase: 02-tenant-fee-management
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/internal/domain/repository/tenant_repository.go
  - apps/api/internal/domain/repository/fee_repository.go
  - apps/api/internal/infrastructure/repository/tenant_repository.go
  - apps/api/internal/infrastructure/repository/fee_repository.go
autonomous: true
requirements:
  - TEN-01
  - FIN-01
  - FIN-02
user_setup: []
must_haves:
  truths:
    - "TenantRepository interface declares Create, FindByID, ListByTerritory, ListByUserID, Update, Delete methods"
    - "FeeRepository interface declares CreateMandatory, CreateMandatoryTx, ListMandatoryByTenant, UpdateMandatory, DeleteMandatory methods"
    - "FeeRepository interface declares CreateVoluntary, ListVoluntaryByTenant, UpdateVoluntary, DeleteVoluntary methods"
    - "PostgresTenantRepository implements all TenantRepository methods with pgx parameterized queries"
    - "PostgresFeeRepository implements all FeeRepository methods with separate mandatory/voluntary SQL"
    - "All tenant repository queries include WHERE territory_id = $1 for RT officers"
    - "All SQL queries use $N parameterized placeholders (not string formatting)"
  artifacts:
    - path: "apps/api/internal/domain/repository/tenant_repository.go"
      provides: "TenantRepository interface with territory-aware CRUD"
      min_lines: 30
    - path: "apps/api/internal/domain/repository/fee_repository.go"
      provides: "FeeRepository interface for mandatory and voluntary fees"
      min_lines: 50
    - path: "apps/api/internal/infrastructure/repository/tenant_repository.go"
      provides: "PostgresTenantRepository pgx implementation"
      min_lines: 160
    - path: "apps/api/internal/infrastructure/repository/fee_repository.go"
      provides: "PostgresFeeRepository pgx implementation"
      min_lines: 200
  key_links:
    - from: "infrastructure/repository/tenant_repository.go"
      to: "domain/repository/tenant_repository.go"
      via: "implements interface"
      pattern: "PostgresTenantRepository.*TenantRepository"
    - from: "infrastructure/repository/tenant_repository.go"
      to: "pgxpool.Pool"
      via: "pool.Query / pool.QueryRow"
      pattern: "r\\.pool\\.(Query|QueryRow|Exec)"
---

<objective>
**Create the repository layer — interfaces and pgx implementations for tenants and fees.**

**Purpose:** Define the data access contracts (interfaces) and implement them with PostgreSQL via pgx, following the exact Clean Architecture pattern established in Phase 1. Every repository method must enforce territory isolation through parameterized WHERE clauses.

**Output:**
- `domain/repository/tenant_repository.go` — interface with territory-aware CRUD
- `domain/repository/fee_repository.go` — interface for mandatory + voluntary fee operations
- `infrastructure/repository/tenant_repository.go` — PostgresTenantRepository pgx implementation
- `infrastructure/repository/fee_repository.go` — PostgresFeeRepository pgx implementation
</objective>

<execution_context>
@/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-tenant-fee-management/01-CONTEXT.md
@.planning/phases/02-tenant-fee-management/01-RESEARCH.md
@.planning/phases/02-tenant-fee-management/02-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

**Domain entity types** (from `apps/api/internal/domain/entity/tenant.go` — created in Plan 01):
- `entity.Tenant` struct
- `entity.MandatoryFee` struct
- `entity.VoluntaryFee` struct

**Existing repository interface pattern** (from `apps/api/internal/domain/repository/user_repository.go`):
- Package: `repository`
- Imports: `"context"`, `"harmoni-api/internal/domain/entity"`
- Method pattern: `MethodName(ctx context.Context, params...) (returnType, error)`
- Doc comments on each method describing behavior and error conditions

**Existing infrastructure repository pattern** (from `apps/api/internal/infrastructure/repository/user_repository.go`):
- Package: `repository`
- Struct: `PostgresXxxRepository` with `pool *pgxpool.Pool`
- Constructor: `NewPostgresXxxRepository(pool *pgxpool.Pool) *PostgresXxxRepository`
- SQL patterns: Raw SQL with `$N` placeholders, `QueryRow.Scan()` for single rows, `rows.Next()` loop for lists
- Error wrapping: `fmt.Errorf("failed to ...: %w", err)`
- `$N` parameterization (NOT string formatting) prevents SQL injection per T-02-03

**Transactional variant pattern** (from `apps/api/internal/infrastructure/repository/password_reset_token_repository.go`):
- Methods accept `pgx.Tx` parameter for transactional operations
- Uses `tx.QueryRow` instead of `r.pool.QueryRow`

**Imports used in user_repository.go:**
```go
import (
    "context"
    "database/sql"
    "fmt"
    "harmoni-api/internal/domain/entity"
    "github.com/jackc/pgx/v5/pgxpool"
)
```

**Claims struct** (used for territory filtering context):
```go
type Claims struct {
    UserID      string
    Role        string
    TerritoryID string
    Expiration  time.Time
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create repository interfaces (TenantRepository, FeeRepository)</name>
  <files>
    apps/api/internal/domain/repository/tenant_repository.go
    apps/api/internal/domain/repository/fee_repository.go
  </files>
  <read_first>
    apps/api/internal/domain/repository/user_repository.go
    apps/api/internal/domain/entity/tenant.go
  </read_first>
  <action>
    Create two interface files in `internal/domain/repository/` following the `user_repository.go` pattern.

    **`tenant_repository.go`:**
    Package `repository`, imports `"context"` and `"harmoni-api/internal/domain/entity"`.

    Interface `TenantRepository` with methods:
    - `Create(ctx context.Context, tenant *entity.Tenant) (*entity.Tenant, error)` — inserts new tenant, returns it with ID/CreatedAt/UpdatedAt populated
    - `CreateTx(ctx context.Context, tx pgx.Tx, tenant *entity.Tenant) (*entity.Tenant, error)` — transactional variant for tenant+fee atomic creation
    - `FindByID(ctx context.Context, id string) (*entity.Tenant, error)` — returns `sql.ErrNoRows` if not found
    - `ListByTerritory(ctx context.Context, territoryID string) ([]*entity.Tenant, error)` — all tenants in a territory, ordered by block, unit_number
    - `ListByUserID(ctx context.Context, userID string) ([]*entity.Tenant, error)` — tenants owned by a resident via user_tenants join, ordered by block, unit_number
    - `Update(ctx context.Context, tenant *entity.Tenant) (*entity.Tenant, error)` — updates block, unit_number, occupancy, monthly_fee for given tenant ID
    - `Delete(ctx context.Context, id string, territoryID string) error` — deletes by id AND territory_id scope (prevents cross-territory delete)

    Each method needs a doc comment describing behavior, territory filtering, and return conditions.

    **`fee_repository.go`:**
    Package `repository`, imports `"context"` and `"harmoni-api/internal/domain/entity"`.

    Interface `FeeRepository` with methods split by fee type per D-03 and D-04:

    Mandatory fees:
    - `CreateMandatory(ctx context.Context, fee *entity.MandatoryFee) (*entity.MandatoryFee, error)`
    - `CreateMandatoryTx(ctx context.Context, tx pgx.Tx, fee *entity.MandatoryFee) (*entity.MandatoryFee, error)` — transactional variant
    - `ListMandatoryByTenant(ctx context.Context, tenantID string) ([]*entity.MandatoryFee, error)` — ordered by effective_date DESC
    - `UpdateMandatory(ctx context.Context, fee *entity.MandatoryFee) (*entity.MandatoryFee, error)` — updates amount, description, effective_date, paid_at
    - `DeleteMandatory(ctx context.Context, id string) error`

    Voluntary fees:
    - `CreateVoluntary(ctx context.Context, fee *entity.VoluntaryFee) (*entity.VoluntaryFee, error)`
    - `ListVoluntaryByTenant(ctx context.Context, tenantID string) ([]*entity.VoluntaryFee, error)` — ordered by effective_date DESC
    - `UpdateVoluntary(ctx context.Context, fee *entity.VoluntaryFee) (*entity.VoluntaryFee, error)`
    - `DeleteVoluntary(ctx context.Context, id string) error`

    Add doc comments on every method describing behavior and edge cases.

    Import `"github.com/jackc/pgx/v5"` in fee_repository.go for the `pgx.Tx` type used in `CreateMandatoryTx`.
  </action>
  <verify>
    <automated>
      cd apps/api && go build ./internal/domain/repository/
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/internal/domain/repository/tenant_repository.go` exports `TenantRepository` interface with all 7 methods
    - `apps/api/internal/domain/repository/fee_repository.go` exports `FeeRepository` interface with all 9 methods
    - Both files compile: `go build ./internal/domain/repository/` exits 0
    - `TenantRepository` has `CreateTx` accepting `pgx.Tx` parameter
    - `TenantRepository` has `ListByUserID` for resident scope
    - `TenantRepository` has `Delete` with `territoryID` parameter
    - `FeeRepository` has separate methods for mandatory vs voluntary (no combined "fee" methods)
  </acceptance_criteria>
  <done>Both repository interfaces created and compile</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement PostgresTenantRepository and PostgresFeeRepository</name>
  <files>
    apps/api/internal/infrastructure/repository/tenant_repository.go
    apps/api/internal/infrastructure/repository/fee_repository.go
  </files>
  <read_first>
    apps/api/internal/infrastructure/repository/user_repository.go
    apps/api/internal/infrastructure/repository/password_reset_token_repository.go
    apps/api/internal/domain/repository/tenant_repository.go
    apps/api/internal/domain/repository/fee_repository.go
  </read_first>
  <action>
    Create two pgx implementation files in `internal/infrastructure/repository/`.
    Package `repository` in both files.

    **`tenant_repository.go` — PostgresTenantRepository:**

    Struct + constructor (following `PostgresUserRepository` pattern):
    ```go
    type PostgresTenantRepository struct {
        pool *pgxpool.Pool
    }

    func NewPostgresTenantRepository(pool *pgxpool.Pool) *PostgresTenantRepository {
        return &PostgresTenantRepository{pool: pool}
    }
    ```

    Implement every method from `domain/repository.TenantRepository`:

    - **Create:** INSERT with RETURNING id, created_at, updated_at. Parameterized `$1-$5`.
    - **CreateTx:** Same INSERT but uses `tx.QueryRow` instead of `r.pool.QueryRow`. Accept `pgx.Tx` from the `"github.com/jackc/pgx/v5"` package.
    - **FindByID:** SELECT all columns WHERE id = $1. Returns sql.ErrNoRows if not found.
    - **ListByTerritory:** SELECT all columns WHERE territory_id = $1 ORDER BY block, unit_number. Territory-filtered per D-02.
    - **ListByUserID:** SELECT all columns with JOIN user_tenants ON tenants.id = user_tenants.tenant_id WHERE user_tenants.user_id = $1 ORDER BY block, unit_number. Resident-scoped per D-06.
    - **Update:** UPDATE block=$1, unit_number=$2, occupancy=$3, monthly_fee=$4, updated_at=NOW() WHERE id=$5. Check `RowsAffected()` — return `sql.ErrNoRows` if 0.
    - **Delete:** DELETE WHERE id=$1 AND territory_id=$2. Check `RowsAffected()` — return `sql.ErrNoRows` if 0. Territory scope prevents cross-territory delete per T-02-01.

    All SQL must use `$N` parameterized placeholders (never string formatting) per T-02-03. All errors wrapped with `fmt.Errorf("failed to ...: %w", err)`.

    **`fee_repository.go` — PostgresFeeRepository:**

    Struct + constructor (same pattern):
    ```go
    type PostgresFeeRepository struct {
        pool *pgxpool.Pool
    }

    func NewPostgresFeeRepository(pool *pgxpool.Pool) *PostgresFeeRepository {
        return &PostgresFeeRepository{pool: pool}
    }
    ```

    Implement every method from `domain/repository.FeeRepository`:

    **Mandatory fee methods:**
    - **CreateMandatory:** INSERT INTO mandatory_fees (tenant_id, amount, description, effective_date, paid_at) VALUES ($1,$2,$3,$4,$5) RETURNING id, created_at
    - **CreateMandatoryTx:** Same INSERT but uses `tx.QueryRow`. Accept `pgx.Tx`.
    - **ListMandatoryByTenant:** SELECT all columns FROM mandatory_fees WHERE tenant_id = $1 ORDER BY effective_date DESC
    - **UpdateMandatory:** UPDATE amount=$1, description=$2, effective_date=$3, paid_at=$4 WHERE id=$5. Check RowsAffected.
    - **DeleteMandatory:** DELETE FROM mandatory_fees WHERE id=$1. Check RowsAffected.

    **Voluntary fee methods:** Identical SQL patterns but against `voluntary_fees` table:
    - **CreateVoluntary:** INSERT INTO voluntary_fees (tenant_id, amount, description, effective_date, paid_at)
    - **ListVoluntaryByTenant:** SELECT FROM voluntary_fees WHERE tenant_id = $1 ORDER BY effective_date DESC
    - **UpdateVoluntary:** UPDATE voluntary_fees SET amount=$1, description=$2, effective_date=$3, paid_at=$4 WHERE id=$5
    - **DeleteVoluntary:** DELETE FROM voluntary_fees WHERE id=$1

    For both files, import `"github.com/jackc/pgx/v5"` for `pgx.Tx` used in transactional variant methods.
    Import `"database/sql"` for `sql.ErrNoRows` returns.

    After creating both files, verify with `cd apps/api && go build ./internal/infrastructure/repository/`.
  </action>
  <verify>
    <automated>
      cd apps/api && go build ./internal/infrastructure/repository/ && go build ./internal/...
    </automated>
  </verify>
  <acceptance_criteria>
    - Both files compile: `go build ./internal/...` exits 0
    - `PostgresTenantRepository` is a struct with `pool *pgxpool.Pool`
    - `NewPostgresTenantRepository` returns `*PostgresTenantRepository`
    - Every method from `TenantRepository` interface is implemented (compiler enforces this)
    - Every method from `FeeRepository` interface is implemented (compiler enforces this)
    - `ListByTerritory` contains `WHERE territory_id = $1` in its SQL
    - `ListByUserID` contains `JOIN user_tenants` in its SQL
    - `Delete` in tenant_repository contains `WHERE id = $1 AND territory_id = $2`
    - All SQL uses `$N` placeholders, no string concatenation or fmt.Sprintf for values
    - `CreateMandatoryTx` and `CreateTx` use `tx.QueryRow` not `r.pool.QueryRow`
  </acceptance_criteria>
  <done>Both repository implementations compile and pass compiler-enforced interface satisfaction</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → PostgreSQL | All queries use pgx parameterized $N placeholders — no SQL injection possible |
| Service → Repository | Service layer passes user claims; repository enforces territory_id filtering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | tenant_repository.go ListByTerritory | mitigate | WHERE territory_id = $1 enforces row-level isolation; Delete checks territory_id too |
| T-02-02 | Elevation of Privilege | fee_repository.go | mitigate | Fee operations reference tenant_id FK — tenant-level scope is enforced by the calling query path |
| T-02-03 | Tampering | All repository SQL queries | mitigate | Every SQL query uses pgx parameterized `$N` placeholders — string formatting is prohibited. This prevents SQL injection per ASVS V5. |
</threat_model>

<verification>
- `cd apps/api && go build ./internal/...` exits 0
- All interface methods are implemented (compiler-enforced)
- No SQL injection vectors (grep for `fmt.Sprintf` in SQL strings — should be 0)
</verification>

<success_criteria>
- PostgresTenantRepository fully implements TenantRepository interface
- PostgresFeeRepository fully implements FeeRepository interface
- All SQL queries use `$N` parameterized placeholders
- Territory isolation is enforced at the query level (WHERE territory_id)
- `go build ./internal/...` succeeds
</success_criteria>

<output>
Create `.planning/phases/02-tenant-fee-management/02-02-SUMMARY.md` when done
</output>
