---
phase: 02-tenant-fee-management
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/internal/domain/repository/tenant_repository.go
  - apps/api/internal/domain/repository/fee_repository.go
  - apps/api/internal/infrastructure/repository/tenant_repository.go
  - apps/api/internal/infrastructure/repository/fee_repository.go
autonomous: true
requirements:
  - TEN-01
  - FIN-01
  - FIN-02

must_haves:
  truths:
    - "TenantRepository interface defines Create, FindByID, ListByTerritory, Update, Delete methods"
    - "FeeRepository interface defines CreateMandatory/CreateVoluntary, List, Update, Delete methods"
    - "PostgresTenantRepository implements TenantRepository with pgx parameterized queries"
    - "PostgresFeeRepository implements FeeRepository with pgx parameterized queries"
    - "All repository queries include territory_id filtering for tenant queries"
    - "Transactional variants exist for atomic tenant+fee creation (CreateMandatoryTx)"
  artifacts:
    - path: "apps/api/internal/domain/repository/tenant_repository.go"
      provides: "TenantRepository interface with CRUD + ListByTerritory + ListByUserID methods"
      contains: "type TenantRepository interface"
    - path: "apps/api/internal/domain/repository/fee_repository.go"
      provides: "FeeRepository interface with mandatory + voluntary fee methods"
      contains: "type FeeRepository interface"
    - path: "apps/api/internal/infrastructure/repository/tenant_repository.go"
      provides: "PostgresTenantRepository implementing TenantRepository"
      contains: "type PostgresTenantRepository struct"
    - path: "apps/api/internal/infrastructure/repository/fee_repository.go"
      provides: "PostgresFeeRepository implementing FeeRepository"
      contains: "type PostgresFeeRepository struct"
  key_links:
    - from: "internal/domain/repository/tenant_repository.go"
      to: "internal/domain/entity/tenant.go"
      via: "import of entity package for Tenant, MandatoryFee, VoluntaryFee types"
    - from: "internal/infrastructure/repository/tenant_repository.go"
      to: "internal/domain/repository/tenant_repository.go"
      via: "implements TenantRepository interface"
    - from: "internal/infrastructure/repository/fee_repository.go"
      to: "internal/domain/repository/fee_repository.go"
      via: "implements FeeRepository interface"
---

<objective>
Create repository interfaces and pgx-backed implementations for tenant and fee data access. Follow the exact patterns established by Phase 1's UserRepository and PasswordResetTokenRepository.

Purpose: Define the data access contract (interface) and provide the PostgreSQL implementation that all service-layer code will depend on. Ensure territory-filtered queries and transactional tenant+fee creation.
Output: 2 interface files + 2 pgx implementation files following Phase 1's repository architecture.
</objective>

<execution_context>
@/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tenant-fee-management/01-CONTEXT.md
@.planning/phases/02-tenant-fee-management/01-RESEARCH.md
@.planning/phases/02-tenant-fee-management/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TenantRepository interface and FeeRepository interface</name>
  <files>
    apps/api/internal/domain/repository/tenant_repository.go
    apps/api/internal/domain/repository/fee_repository.go
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/internal/domain/repository/user_repository.go (existing interface pattern — package repository, import entity, method signatures with ctx and error returns)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/internal/domain/repository/password_reset_token_repository.go (existing alternate interface pattern — non-entity struct in same package, Tx variants)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/02-tenant-fee-management/02-PATTERNS.md (lines 84-152: exact interface patterns for TenantRepository and FeeRepository)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/internal/domain/entity/tenant.go (entity types this interface references)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/02-tenant-fee-management/01-CONTEXT.md (lines 14-16: data isolation via territory_id; lines 19-22: separate fee tables; lines 29-36: resident access via user_tenants)
  </read_first>
  <action>
    Create `tenant_repository.go` in package `repository` with imports for `"context"` and `"harmoni-api/internal/domain/entity"`.

    Define `TenantRepository` interface with these methods (exact signatures per PATTERNS.md lines 99-115):
    - `Create(ctx context.Context, tenant *entity.Tenant) (*entity.Tenant, error)` — standard create
    - `CreateTx(ctx context.Context, tx pgx.Tx, tenant *entity.Tenant) (*entity.Tenant, error)` — transactional variant for atomic tenant+fee creation. Import `"github.com/jackc/pgx/v5"` for pgx.Tx.
    - `FindByID(ctx context.Context, id string) (*entity.Tenant, error)` — returns sql.ErrNoRows if not found
    - `ListByTerritory(ctx context.Context, territoryID string) ([]*entity.Tenant, error)` — territory-filtered list for RT/RW officers
    - `ListByUserID(ctx context.Context, userID string) ([]*entity.Tenant, error)` — joins through user_tenants for resident role
    - `Update(ctx context.Context, tenant *entity.Tenant) (*entity.Tenant, error)` — full update with territory_id guard
    - `Delete(ctx context.Context, id string, territoryID string) error` — delete with territory scoping; verify RowsAffected

    Create `fee_repository.go` in package `repository` with imports for `"context"`, `"harmoni-api/internal/domain/entity"`, and `"github.com/jackc/pgx/v5"`.

    Define `FeeRepository` interface with separate method groups for mandatory and voluntary fees (per CONTEXT.md D-02 — separate tables):

    Mandatory fee methods:
    - `CreateMandatory(ctx context.Context, fee *entity.MandatoryFee) (*entity.MandatoryFee, error)`
    - `CreateMandatoryTx(ctx context.Context, tx pgx.Tx, fee *entity.MandatoryFee) (*entity.MandatoryFee, error)` — transactional for atomic tenant+fee creation
    - `ListMandatoryByTenant(ctx context.Context, tenantID string) ([]*entity.MandatoryFee, error)`
    - `UpdateMandatory(ctx context.Context, fee *entity.MandatoryFee) (*entity.MandatoryFee, error)`
    - `DeleteMandatory(ctx context.Context, id string) error`

    Voluntary fee methods:
    - `CreateVoluntary(ctx context.Context, fee *entity.VoluntaryFee) (*entity.VoluntaryFee, error)`
    - `ListVoluntaryByTenant(ctx context.Context, tenantID string) ([]*entity.VoluntaryFee, error)`
    - `UpdateVoluntary(ctx context.Context, fee *entity.VoluntaryFee) (*entity.VoluntaryFee, error)`
    - `DeleteVoluntary(ctx context.Context, id string) error`

    Follow the exact UserRepository pattern: package `repository`, doc comments on each method, error returns wrapped with `fmt.Errorf`.
  </action>
  <acceptance_criteria>
    - Both files compile: `cd apps/api && go build ./internal/domain/repository/...`
    - `TenantRepository` has CreateTx method accepting pgx.Tx for transactional creation
    - `FeeRepository` separates mandatory from voluntary methods
    - All methods use `context.Context` as first parameter per Phase 1 convention
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && go build ./internal/domain/repository/...</automated>
  </verify>
  <done>Two interface files compile. TenantRepository has 7 methods. FeeRepository has 9 methods (4 mandatory + 4 voluntary + 1 CreateMandatoryTx).</done>
</task>

<task type="auto">
  <name>Task 2: Create PostgresTenantRepository implementing TenantRepository</name>
  <files>
    apps/api/internal/infrastructure/repository/tenant_repository.go
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/internal/infrastructure/repository/user_repository.go (existing pgx repository pattern — pool-based struct, raw SQL with $N placeholders, rows.Scan, error wrapping, territory filtering)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/internal/domain/repository/tenant_repository.go (interface this must implement)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/internal/domain/entity/tenant.go (entity types)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/02-tenant-fee-management/02-PATTERNS.md (lines 296-411: exact pgx repository patterns for PostgresTenantRepository)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/02-tenant-fee-management/01-RESEARCH.md (lines 402-429: territory-filtered query example; lines 339-398: transactional tenant+fee creation)
  </read_first>
  <action>
    Create `internal/infrastructure/repository/tenant_repository.go` in package `repository` implementing the `repository.TenantRepository` interface.

    Struct + constructor pattern (exact analog of PostgresUserRepository lines 13-21):
    ```go
    type PostgresTenantRepository struct { pool *pgxpool.Pool }
    func NewPostgresTenantRepository(pool *pgxpool.Pool) *PostgresTenantRepository { ... }
    ```

    Implement ALL interface methods with parameterized pgx SQL queries. Key implementation notes per RESEARCH.md pitfalls:

    1. **Create** — INSERT with `RETURNING id, created_at, updated_at`. monthly_fee stored as `numeric` in DB, scanned into `decimal.Decimal` via pgx's built-in numeric support.
    
    2. **CreateTx** — Same INSERT but uses `tx.QueryRow(ctx, query, ...)` instead of `r.pool.QueryRow`. This runs inside the service-layer transaction for atomic tenant+fee creation.

    3. **FindByID** — SELECT with `WHERE id = $1`. Returns `sql.ErrNoRows` when not found. Scan field for monthly_fee must handle decimal.Decimal.

    4. **ListByTerritory** — SELECT with `WHERE territory_id = $1 ORDER BY block, unit_number`. Territory filtering per RESEARCH.md territory isolation pitfall (pitfall 1). This is the primary query for RT/RW officers.

    5. **ListByUserID** — SELECT joining `user_tenants` junction table: `FROM tenants t JOIN user_tenants ut ON t.id = ut.tenant_id WHERE ut.user_id = $1 ORDER BY t.block, t.unit_number`. This is the resident-scoped query per D-04.

    6. **Update** — UPDATE with `WHERE id = $1 AND territory_id = $2`. Return `sql.ErrNoRows` if 0 rows affected (cross-territory update attempt). All fields updated.

    7. **Delete** — DELETE with `WHERE id = $1 AND territory_id = $2`. Check `RowsAffected()` — return `sql.ErrNoRows` if 0.

    All monthly_fee fields use decimal.Decimal. Use pgx's native numeric scanning: pgx v5 supports scanning NUMERIC into decimal.Decimal via the pgtype system. Import `"github.com/shopspring/decimal"` and ensure the scan handles the conversion. If pgx native scanning requires a wrapper, use `pgtype.Numeric` and convert via `decimal.NewFromString()`.

    Follow the exact error wrapping pattern from user_repository.go: `fmt.Errorf("failed to ...: %w", err)`.
  </action>
  <acceptance_criteria>
    - `cd apps/api && go build ./internal/infrastructure/repository/...` compiles
    - Compile-time interface check: `var _ repository.TenantRepository = (*PostgresTenantRepository)(nil)` passes
    - All 7 methods implemented with parameterized SQL ($N placeholders, no string concatenation)
    - Territory filtering in ListByTerritory, Update, and Delete methods via `WHERE territory_id = $N`
    - ListByUserID joins user_tenants junction table
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && go build ./internal/infrastructure/repository/...</automated>
  </verify>
  <done>PostgresTenantRepository implements all 7 TenantRepository methods. Compile-time interface assertion passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create PostgresFeeRepository implementing FeeRepository</name>
  <files>
    apps/api/internal/infrastructure/repository/fee_repository.go
  </files>
  <read_first>
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/internal/infrastructure/repository/password_reset_token_repository.go (existing pgx repository pattern for non-CRUD entity with Tx variant)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/internal/domain/repository/fee_repository.go (interface this must implement)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/apps/api/internal/domain/entity/tenant.go (MandatoryFee and VoluntaryFee entity types)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/02-tenant-fee-management/02-PATTERNS.md (lines 414-422: fee repository pattern, separate mandatory vs voluntary methods)
    @/Users/bonanugroho/projects/vibing/harmoni/harmoni-app/.planning/phases/02-tenant-fee-management/01-CONTEXT.md (lines 19-22: mandatory_fees and voluntary_fees are separate tables)
  </read_first>
  <action>
    Create `internal/infrastructure/repository/fee_repository.go` in package `repository` implementing the `repository.FeeRepository` interface.

    Struct + constructor pattern:
    ```go
    type PostgresFeeRepository struct { pool *pgxpool.Pool }
    func NewPostgresFeeRepository(pool *pgxpool.Pool) *PostgresFeeRepository { ... }
    ```

    Add compile-time interface check: `var _ repository.FeeRepository = (*PostgresFeeRepository)(nil)`

    Implement ALL interface methods with separate SQL for mandatory_fees and voluntary_fees tables:

    **Mandatory fee methods:**
    - `CreateMandatory` — INSERT INTO mandatory_fees (tenant_id, amount, description, effective_date, paid_at) VALUES ($1, $2, $3, $4, $5) RETURNING id, created_at, updated_at
    - `CreateMandatoryTx` — Same INSERT using `tx.QueryRow` for transactional use within tenant creation (per CONTEXT.md D-05: "at least one mandatory fee per tenant")
    - `ListMandatoryByTenant` — SELECT from mandatory_fees WHERE tenant_id = $1 ORDER BY effective_date DESC
    - `UpdateMandatory` — UPDATE mandatory_fees SET amount=$2, description=$3, effective_date=$4, paid_at=$5, updated_at=NOW() WHERE id=$1
    - `DeleteMandatory` — DELETE FROM mandatory_fees WHERE id = $1

    **Voluntary fee methods:**
    - `CreateVoluntary` — INSERT INTO voluntary_fees (tenant_id, amount, description, effective_date, paid_at) VALUES ($1, $2, $3, $4, $5) RETURNING id, created_at, updated_at
    - `ListVoluntaryByTenant` — SELECT from voluntary_fees WHERE tenant_id = $1 ORDER BY effective_date DESC
    - `UpdateVoluntary` — UPDATE voluntary_fees SET amount=$2, description=$3, effective_date=$4, paid_at=$5, updated_at=NOW() WHERE id=$1
    - `DeleteVoluntary` — DELETE FROM voluntary_fees WHERE id = $1

    For Update methods: check `RowsAffected() == 0` and return `sql.ErrNoRows` if no rows affected.
    For paid_at: use `*time.Time` — scan handles NULL into nil pointer for unpaid fees.

    All amount fields use `decimal.Decimal`. Follow the same pgx NUMERIC scanning approach as tenant_repository.go.
  </action>
  <acceptance_criteria>
    - `cd apps/api && go build ./internal/infrastructure/repository/...` compiles
    - Compile-time interface check passes: `var _ repository.FeeRepository = (*PostgresFeeRepository)(nil)`
    - All 9 method implementations exist (4 mandatory + 4 voluntary + 1 CreateMandatoryTx)
    - CreateMandatoryTx uses pgx.Tx for transactional creation within tenant create flow
    - All SQL uses $N parameterized placeholders — no string concatenation
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && go build ./internal/...</automated>
  </verify>
  <done>PostgresFeeRepository implements all 9 FeeRepository methods with separate mandatory/voluntary SQL. Interface assertion passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Database | All SQL passes through pgx parameterized queries ($N placeholders). No raw string interpolation. |
| Service → Repository | Service layer passes territoryID from JWT claims. Repository never accepts territoryID from request body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | ListByTerritory / Update / Delete | mitigate | Every tenant query includes `WHERE territory_id = $N` parameter. Methods accepting tenant ID also accept territory ID for cross-validation. |
| T-02-02 | Elevation of Privilege | FeeRepository | mitigate | Fee methods do not include territory_id directly — they rely on tenant_id FK relationship. TenantRepository enforces territory scoping. Fee service must validate fee access through parent tenant's territory. |
| T-02-03 | Tampering | All repository methods | mitigate | All SQL uses pgx parameterized `$N` placeholders. No fmt.Sprintf or string concatenation for query building. Pattern identical to Phase 1's user_repository.go. |
| T-02-SC | Tampering | Repository compile-time checks | mitigate | Go's `var _ Interface = (*Impl)(nil)` compile-time assertion ensures implementations match interfaces. `go vet` catches type mismatches. |
</threat_model>

<verification>
- `cd apps/api && go build ./internal/...` passes
- Compile-time interface assertions pass for both repositories
- `go vet ./internal/...` passes
</verification>

<success_criteria>
1. TenantRepository interface defined with 7 methods including CreateTx for atomic creation
2. FeeRepository interface defined with 9 methods separating mandatory/voluntary
3. PostgresTenantRepository implements all TenantRepository methods with territory-filtered SQL
4. PostgresFeeRepository implements all FeeRepository methods with parameterized SQL
5. All queries use $N parameterized placeholders (no SQL injection vectors)
6. `go build ./internal/...` succeeds
</success_criteria>

<output>
Create `.planning/phases/02-tenant-fee-management/02-02-SUMMARY.md` when done
</output>
